<!-- block:zh -->
## 解决方法

> [!TIP]
> 建议更改仓库名为：
> ```
> {suggested_repo_name}
> ```

1. **修改名称**：前往 **[Settings](../../settings)** --> **Repository name**，将仓库名重命名为上方名称并保存。
2. **重新触发**：进入 **[Actions](../../actions/workflows/init.yml)** 页面 --> 选择 **"初始化 (Initialize)"** 工作流 --> 点击 **Run workflow** 运行。
<!-- /block:zh -->
---

<!-- block:en -->

<!-- block:en -->
## How to Fix

> [!TIP]
> Suggested repository name:
> ```
> {suggested_repo_name}
> ```

1. **Rename**: Go to **[Settings](../../settings)** --> **Repository name**, rename the repository to the name above and save.
2. **Re-trigger**: Go to **[Actions](../../actions/workflows/init.yml)** --> select the **"Initialize"** workflow --> click **Run workflow**.
<!-- /block:en -->
